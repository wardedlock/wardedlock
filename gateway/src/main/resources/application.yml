spring:
  application:
    name: gateway
  config:
    import:
      - optional:file:.env
      - optional:file:../.env
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: ${AUTH_SERVICE_URI}
          predicates:
            - Path=/auth/**

server:
  port: ${GATEWAY_SERVICE_PORT}
